<nav class="top-bar">
  <ul class="title-area">
    <!-- Title Area -->
    <li class="name">
      <h1><%= link_to("phtlbm", root_path) %></h1>
    </li>
    <!-- Remove the class "menu-icon" to get rid of menu icon. Take out "Menu" to just have icon alone -->
    <li class="toggle-topbar menu-icon"><a href="#"><span>Menu</span></a></li>
  </ul>

  <section class="top-bar-section">
    <!-- Left Nav Section -->
<!--     <ul class="left">
      <li class="divider"></li>
      <li><%#= link_to("Exercises", root_path ) %>
      <li class="divider"></li>
      <li><%#= link_to("Graphs", '#') %>
      <li class="divider"></li>
    </ul> -->

    <!-- Right Nav Section -->
    <ul class="right">
      <% if @auth %>
      <li class="divider"></li>
      <li><%= link_to("Profile", edit_subscriber_path(@auth.sub) ) %>
      <li class="divider"></li>
      <li><%= link_to("#{ @auth.username }", '#' ) %>
      <li class="divider"></li>
      <li><%= link_to("Logout", login_path, :method => 'delete', :remote => true ) %>
      <li class="divider"></li>
      <% end %>
    </ul>
  </section>
</nav>